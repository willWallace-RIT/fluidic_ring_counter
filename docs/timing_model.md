# Timing Model

Each NOT gate introduces a delay tau_i.

Total loop delay:

T = sum(tau_i)

Oscillation period:

T_osc approx 2 * sum(tau_i)

---

## Design implication

- Increasing channel length increases delay
- Asymmetry creates phase shifts between outputs
- Non-uniform delays create traveling wave behavior
